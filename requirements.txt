# cboe_data 수집 루틴의 최소 의존성. (표준 라이브러리 urllib로 통신하므로
# HTTP 라이브러리는 불필요. pandas는 exchange_calendars가 요구.)
# tzdata: 슬림 리눅스 샌드박스에는 system zoneinfo가 없어 zoneinfo.ZoneInfo가
#         깨질 수 있으므로 명시적으로 포함한다.
pandas>=2.1
exchange_calendars>=4.5
tzdata>=2024.1
